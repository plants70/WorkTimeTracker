name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"   # стабильнее для тулов, чем 3.13

      - name: Install dev dependencies only
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; fi

      - name: Ruff (lint)
        run: python -m ruff check .

      - name: Black (format check)
        run: python -m black --check .

  # Тесты пока отключены, чтобы не тянуть тяжелые зависимости (PyQt5 и т.п.)
  # Позже включим, когда появятся юнит-тесты без GUI.
  # tests:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-python@v5
  #       with:
  #         python-version: "3.12"
  #     - run: |
  #         python -m pip install --upgrade pip
  #         pip install -r requirements-dev.txt
  #     - run: |
  #         if [ -d tests ]; then pytest -q; else echo "no tests"; fi
